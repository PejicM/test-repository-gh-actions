name: Backslashes on Windows with bash as shell
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: windows-latest
    steps:
      - run: echo "Hello world!"
        shell: bash
      - run: ls
        shell: bash
