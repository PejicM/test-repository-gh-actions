name: Backslashes on Windows with bash as shell
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: self-hosted
    steps:
      - run: echo "Hello world!"
        shell: bash
      - run: ls
        shell: bash
